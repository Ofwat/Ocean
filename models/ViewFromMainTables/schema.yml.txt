version: 2

sources:
  - name: abc
    description: wedfwqe
    tables:
      - name: PR14FinalCSVcreatedbyPythonSource
        description: One record per order. Includes cancelled and deleted orders.
        columns:
          - name: unique_id
            tests:
              - not_null
          - name: company_type
            tests:
              - not_null